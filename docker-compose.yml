version: "3"

services:
  google-play-scraper:
    image: node:14-alpine
    container_name: google-play-scraper
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - 3000:3000
    command: yarn start